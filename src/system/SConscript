####################################
# SCons build system for Pedigree
## Tyler Kennedy (AKA Linuxhq AKA TkTech)
####################################

import os.path
Import(['env'])

subdirs = ['boot', 'common', 'kernel']

SConscript([os.path.join(i, 'SConscript') for i in subdirs],exports = ['env'])
